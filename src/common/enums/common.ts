export enum EUserRole {
  ADMIN = "admin",
  STUDENT = "student",
  PROFESSOR = "professor",
  DIRECTOR = "director",
  PARENT = "parent",
}

export enum EUserGenre {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other"
}

export enum EHistoryType {
  USER = "User",
  PRODUCT = "Product",
}
